# Shared rate-limit zones for meetings-cal (mc.spica.mistxs.ru)
limit_req_zone $binary_remote_addr zone=mc_general:10m rate=15r/s;
limit_req_zone $binary_remote_addr zone=mc_api:10m rate=8r/s;
limit_req_zone $binary_remote_addr zone=mc_auth:10m rate=1r/s;
limit_conn_zone $binary_remote_addr zone=mc_conn:10m;
